import { FeatureSelectionChangeEvent } from './FeatureSelectionChangeEvent';

/**
 * handler for the feature selection change event
 */
export type FeatureSelectionChangeEventHandler = (event: FeatureSelectionChangeEvent) => void;
