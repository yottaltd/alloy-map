import { AlloyMap } from '@/map/core/AlloyMap';

/**
 * options for the alloy draw layer
 */
export interface AlloyDrawLayerOptions {
  /**
   * the alloy map to associate tha layer with
   */
  map: AlloyMap;
}
