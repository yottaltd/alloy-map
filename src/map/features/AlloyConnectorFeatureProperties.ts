/**
 * the properties for an alloy connector feature
 * @ignore
 * @internal
 */
export interface AlloyConnectorFeatureProperties {
  /**
   * the colour to represent the feature
   */
  readonly colour: string;
}
