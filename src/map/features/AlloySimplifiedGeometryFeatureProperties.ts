/**
 * the properties returned from the service for an alloy simplified geometry feature
 */
export interface AlloySimplifiedGeometryFeatureProperties {
  /**
   * its stringified type
   */
  readonly type: string;

  /**
   * the style id that includes the feature
   */
  readonly styleId: string;
}
