/**
 * the properties for an alloy path node connector feature
 * @ignore
 * @internal
 */
export interface AlloyPathNodeConnectorFeatureProperties {
  /**
   * the colour to represent the feature
   */
  readonly colour: string;
}
