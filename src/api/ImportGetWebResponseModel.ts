// tslint:disable
import { ImportWebModel } from './ImportWebModel';
/**
 * Get web model for import
 * @export
 * @interface ImportGetWebResponseModel
 */
export interface ImportGetWebResponseModel {
  /**
   * The import returned as a result
   * @type {ImportWebModel}
   * @memberof ImportGetWebResponseModel
   */
  import: ImportWebModel;
}
