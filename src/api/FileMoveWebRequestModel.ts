
/**
 * Web request model for a file move operation
 * @export
 * @interface FileMoveWebRequestModel
 */
export interface FileMoveWebRequestModel {
  /**
   * The AId of the item representing the folder to move file to
   * @type {string}
   * @memberof FileMoveWebRequestModel
   */
  folderItemId: string;
}
